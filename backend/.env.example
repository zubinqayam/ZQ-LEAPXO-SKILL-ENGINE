PORT=8000
# Comma-separated list of allowed origins (leave empty to allow all in dev).
# FastAPI (backend/main.py) and Express (backend/src/server.js) both read this.
# Example: CORS_ALLOWED_ORIGINS=https://app.leapxo.example.com
CORS_ALLOWED_ORIGINS=
